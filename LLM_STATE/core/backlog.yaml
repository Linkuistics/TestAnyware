tasks:
- id: fix-open-menu-docs-example-file-open-recent-not-file-recent-files
  title: 'Fix --open-menu docs example: File,Open Recent not File,Recent Files'
  category: bug
  status: not_started
  dependencies: []
  description: |
    Fix `--open-menu` docs example: use `"File,Open Recent"` not `"File,Recent Files"`.

    The `--open-menu` flag documentation (and the original `smoke-test-window-compensation-and-open-menu-path-on-tahoe-golden` task description) uses `--open-menu "File,Recent Files"` as a sample path. On macOS Tahoe the actual submenu is named "Open Recent", not "Recent Files". The smoke test confirmed this: `--open-menu "File,Recent Files"` errors with "No menu item matching 'Recent Files'".

    **Files to update:**
    - The `--open-menu` help string in `cli/Sources/testanyware/AgentCommand.swift` (if it names the submenu).
    - Any inline code comments or docstrings referencing `"File,Recent Files"`.
    - `0-docs/` plan files or README sections that quote this example (search for `Recent Files`).
- id: fix-startstoproundtripwindows-integration-test-600-s-timeout
  title: Fix startStopRoundTripWindows() integration test 600 s timeout
  category: bug
  status: not_started
  dependencies: []
  description: |
    Fix `startStopRoundTripWindows()` integration test timeout: 600 s is too tight for cold Windows VM boot.

    A fresh Windows clone needs 8–12 min for first-logon + Task Scheduler + agent startup. The current 600 s (`swift test`) timeout fires before the agent is ready, producing a spurious failure that is unrelated to any code defect.

    **Options (pick one):**
    1. Bump the test timeout to 900 s (simplest fix; matches observed worst-case ~8 min + headroom).
    2. Split the test into a slow "boot" phase (new `startWindowsVm()` helper with 900 s timeout) and a fast "API roundtrip" phase that asserts after the boot completes. Lets CI report the phases separately.

    Whichever option is chosen, the 2 pre-existing QEMU swtpm failures remain; a passing suite shows 20/21 non-Windows + 1/1 Windows = 21/21 (or 270+1=271/272 if QEMU tests are included in the same run).

    **Surfaced during:** P6 end-to-end smoke test integration suite run (step 12), where the Windows test was the sole failure.
- id: investigate-agent-inspect-role-returning-multiple-elements-matched-when-window-is-scoped
  title: Investigate agent inspect --role returning Multiple elements matched when --window is scoped
  category: bug
  status: not_started
  dependencies: []
  description: |
    Investigate and fix `agent inspect --role <role>` returning "Multiple elements matched" even when `--window` is also passed.

    **Bug:** During the P6 `--window` smoke-test on the macOS Tahoe clone, `agent inspect --role <role> --window <name>` errored with "Multiple elements matched" even though `--window` was intended to scope the search to a single window. The window scope apparently does not deduplicate elements that appear in both the text-area and scroll-bar subtrees of the AX role tree.

    **Investigation starting points:**
    - `cli/Sources/TestAnywareDriver/Agent/` — the `inspect` command's `--window` filtering logic.
    - `AXElementWrapper` / AX walker — check whether scroll-bar children inherit the same role as the text area.
    - Reproduce: open TextEdit in the macOS golden clone, then `testanyware agent inspect --role AXTextArea --window "Untitled"`.

    **Acceptance:** `agent inspect --role AXTextArea --window "Untitled"` returns a single match (the text area), not an error. If there are legitimately multiple role matches in a window, the error message should tell the user to add `--index N` rather than failing silently.
- id: virtiofs-serves-stale-cached-files-after-host-update
  title: VirtioFS serves stale cached files after host update
  category: bug
  status: not_started
  dependencies: []
  description: |
    VirtioFS shared filesystem can serve cached (stale) file content
    to the VM even after the host has written an updated version. Current workaround:
    use base64 transfer (`testanyware scp` or inline base64) or restart the VM. A
    `testanyware sync` command or explicit cache-bust mechanism (e.g. `testanyware scp
    --no-virtio-cache`) would eliminate this class of silent transfer error. Affects
    any workflow that edits a source file on the host and then recompiles or re-runs it
    inside the VM. Surfaced on 2026-04-17 during Racket sample app validation.
- id: extend-doctor-verify-bundled-agents-exist-and-are-executable
  title: 'Extend doctor: verify bundled agents exist and are executable'
  category: feature
  status: done
  dependencies: []
  description: |
    Extend `testanyware doctor` to verify that the per-platform bundled agent binaries exist and are executable under the Homebrew prefix.

    **Background:** The `diagnostics-module-uses-classify-run-split-for-testability` memory entry documents the existing doctor check architecture (classify/run split in `cli/Sources/TestAnywareDriver/Diagnostics/`). The `golden-scripts-resolve-agent-cli-from-brew-prefix-by-default` memory entry documents the expected agent paths.

    **Check to add:**
    - Verify `$(brew --prefix testanyware)/share/testanyware/agents/macos/testanyware-agent` exists and is executable.
    - Verify `$(brew --prefix testanyware)/share/testanyware/agents/linux/testanyware-agent` exists and is executable.
    - Verify `$(brew --prefix testanyware)/share/testanyware/agents/windows/testanyware-agent.exe` exists.

    **Implementation:** Add `BundledAgentCheck.swift` in `cli/Sources/TestAnywareDriver/Diagnostics/` using the classify/run split pattern. Wire into `DoctorCommand.swift`. Add unit tests alongside `InstallPathCheckTests.swift`.

    **Promoted from:** `add-doctor-check-for-usr-local-bin-testanyware-dev-symlink-shadowing-brew-install` — "## What this suggests next" item (b).
  results: |
    ## What was done

    Added `BundledAgentsCheck` to `cli/Sources/TestAnywareDriver/Diagnostics/`
    mirroring the classify/run split established by `InstallPathCheck`. The
    check verifies three slots under `<brewPrefix>/share/testanyware/agents/`:

    - `macos/testanyware-agent` — exists and is marked executable
    - `windows/testanyware-agent.exe` — exists (presence-only; the win-arm64
      binary won't carry the macOS executable bit and we should not assert one)
    - `linux/testanyware_agent/__main__.py` — Python package entry-point
      marker (the Linux agent ships as a directory tree, not a binary)

    `BundledAgentsCheck.classify(brewPrefix:probes:)` is pure: it takes
    `SlotProbe(slot, expectedPath, exists, isExecutable)` per platform and
    returns `Verdict` (`allPresent`, `missing(brewPrefix, issues)`, or
    `noHomebrew`). `BundledAgentsCheck.run()` resolves the brew prefix via
    `brew --prefix`, probes the three paths via `FileManager`, and calls
    `classify`. Wired into `DoctorCommand.run()` between the install-path
    section and host-tool section.

    Unit tests in `BundledAgentsCheckTests.swift` cover all-present, missing
    binary, present-but-not-executable (macOS-only assertion), windows .exe
    presence-only semantics, linux package marker presence-only semantics,
    multi-platform missing accumulation, expected-path lockstep with the
    release bundle layout, and `isOK` polarity for each verdict. All 11 new
    tests pass alongside the existing 15.

    ## Verification

    - `swift build` (cli/) — clean, 9.12s.
    - `swift test --filter "BundledAgentsCheckTests|ToolAvailabilityCheckTests|InstallPathCheckTests"` — 26/26 pass.
    - `testanyware doctor` against the live install (brew prefix
      `/opt/homebrew`) reports `✓ macOS, Windows, and Linux agents all
      present` with the bundle root printed.

    ## What this suggests next

    - `share/testanyware/scripts/` (vm-*.sh) and `share/testanyware/helpers/`
      (autounattend.xml etc.) are also bundled by `release-build.sh` but
      not currently verified by doctor. A future task could extend
      `BundledAgentsCheck` (or split out a sibling `BundledScriptsCheck`)
      to cover them — the same pattern applies. Not added in this task to
      stay scoped to the explicit "agents" wording of the request.
- id: extend-doctor-verify-tart-and-qemu-tool-availability
  title: 'Extend doctor: verify tart and qemu tool availability'
  category: feature
  status: done
  dependencies: []
  description: |
    Extend `testanyware doctor` to verify that `tart` and `qemu-system-aarch64` are discoverable at the paths expected by `TartRunner` and `QEMURunner`.

    **Background:** `TartRunner` and `QEMURunner` rely on specific tool paths. A missing or mislocated binary causes cryptic runtime failures at VM start time. Doctor can surface this hazard at setup time.

    **Check to add:**
    - Verify `tart` is available (mirror the path logic in `TartRunner`).
    - Verify `qemu-system-aarch64` is available (mirror the homebrew fallback path used by `QEMURunner.start`).

    **Implementation:** Add `ToolAvailabilityCheck.swift` in `cli/Sources/TestAnywareDriver/Diagnostics/` using the classify/run split pattern. Wire into `DoctorCommand.swift`. Add unit tests alongside `InstallPathCheckTests.swift`.

    **Promoted from:** `add-doctor-check-for-usr-local-bin-testanyware-dev-symlink-shadowing-brew-install` — "## What this suggests next" item (c).
  results: |
    ## What was done

    Added `ToolAvailabilityCheck` to `cli/Sources/TestAnywareDriver/Diagnostics/`
    following the same classify/run split. The check probes three host
    binaries via `/usr/bin/which`:

    - `tart` — macOS and Linux VMs
    - `qemu-system-aarch64` — Windows VMs
    - `swtpm` — TPM 2.0 emulation for Windows 11 VMs

    `ToolAvailabilityCheck.classify(resolve:tools:)` is pure: it takes a
    `(String) -> String?` resolver and returns one `Status` entry per known
    tool, in `knownTools` order. `run()` instantiates the resolver as a
    local `which` helper. Each `Tool` carries its purpose and an install
    hint — surfaced verbatim in the doctor output when a tool is missing.

    Tool availability is **advisory**: missing tools do not flip `isOK`. A
    user running only macOS GUI tests has no use for QEMU or swtpm, and
    failing the doctor on their absence would cry wolf on a serviceable
    install. Missing tools surface with `!` (warning) and the install hint;
    present tools surface with `✓` and the resolved path.

    Wired into `DoctorCommand.run()` as a third section after install-path
    and bundled-agents. The doctor exits non-zero only when install-path or
    bundled-agents flag a hard issue.

    Unit tests in `ToolAvailabilityCheckTests.swift` cover all-present,
    all-missing, partial availability, stable reporting order, and the
    "every known tool carries non-empty purpose + install hint" invariant.
    All 5 new tests pass.

    ## Verification

    - `swift build` (cli/) — clean.
    - `swift test --filter ToolAvailabilityCheckTests` — 5/5 pass; combined
      with `BundledAgentsCheckTests` and `InstallPathCheckTests`, 26/26
      pass.
    - `testanyware doctor` against the live install reports all three tools
      resolved (`tart`, `qemu-system-aarch64`, `swtpm` under
      `/opt/homebrew/bin/`).

    ## What this suggests next

    - The `--version` of each resolved tool isn't checked. `tart` and
      `qemu-system-aarch64` have known-incompatible-version hazards
      (release notes mention version-specific behavior in
      `provisioner/scripts/`). A version-floor check could go on top of
      the path resolution.
    - `dotnet` (for contributor-only Windows agent builds) is intentionally
      not checked — it's not a runtime dep. If contributor tooling ever
      gets its own doctor target, it belongs there, not here.
