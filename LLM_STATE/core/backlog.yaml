tasks:
- id: fix-open-menu-docs-example-file-open-recent-not-file-recent-files
  title: 'Fix --open-menu docs example: File,Open Recent not File,Recent Files'
  category: bug
  status: not_started
  dependencies: []
  description: |
    Fix `--open-menu` docs example: use `"File,Open Recent"` not `"File,Recent Files"`.

    The `--open-menu` flag documentation (and the original `smoke-test-window-compensation-and-open-menu-path-on-tahoe-golden` task description) uses `--open-menu "File,Recent Files"` as a sample path. On macOS Tahoe the actual submenu is named "Open Recent", not "Recent Files". The smoke test confirmed this: `--open-menu "File,Recent Files"` errors with "No menu item matching 'Recent Files'".

    **Files to update:**
    - The `--open-menu` help string in `cli/Sources/testanyware/AgentCommand.swift` (if it names the submenu).
    - Any inline code comments or docstrings referencing `"File,Recent Files"`.
    - `0-docs/` plan files or README sections that quote this example (search for `Recent Files`).
- id: fix-startstoproundtripwindows-integration-test-600-s-timeout
  title: Fix startStopRoundTripWindows() integration test 600 s timeout
  category: bug
  status: not_started
  dependencies: []
  description: |
    Fix `startStopRoundTripWindows()` integration test timeout: 600 s is too tight for cold Windows VM boot.

    A fresh Windows clone needs 8–12 min for first-logon + Task Scheduler + agent startup. The current 600 s (`swift test`) timeout fires before the agent is ready, producing a spurious failure that is unrelated to any code defect.

    **Options (pick one):**
    1. Bump the test timeout to 900 s (simplest fix; matches observed worst-case ~8 min + headroom).
    2. Split the test into a slow "boot" phase (new `startWindowsVm()` helper with 900 s timeout) and a fast "API roundtrip" phase that asserts after the boot completes. Lets CI report the phases separately.

    Whichever option is chosen, the 2 pre-existing QEMU swtpm failures remain; a passing suite shows 20/21 non-Windows + 1/1 Windows = 21/21 (or 270+1=271/272 if QEMU tests are included in the same run).

    **Surfaced during:** P6 end-to-end smoke test integration suite run (step 12), where the Windows test was the sole failure.
- id: investigate-agent-inspect-role-returning-multiple-elements-matched-when-window-is-scoped
  title: Investigate agent inspect --role returning Multiple elements matched when --window is scoped
  category: bug
  status: not_started
  dependencies: []
  description: |
    Investigate and fix `agent inspect --role <role>` returning "Multiple elements matched" even when `--window` is also passed.

    **Bug:** During the P6 `--window` smoke-test on the macOS Tahoe clone, `agent inspect --role <role> --window <name>` errored with "Multiple elements matched" even though `--window` was intended to scope the search to a single window. The window scope apparently does not deduplicate elements that appear in both the text-area and scroll-bar subtrees of the AX role tree.

    **Investigation starting points:**
    - `cli/Sources/TestAnywareDriver/Agent/` — the `inspect` command's `--window` filtering logic.
    - `AXElementWrapper` / AX walker — check whether scroll-bar children inherit the same role as the text area.
    - Reproduce: open TextEdit in the macOS golden clone, then `testanyware agent inspect --role AXTextArea --window "Untitled"`.

    **Acceptance:** `agent inspect --role AXTextArea --window "Untitled"` returns a single match (the text area), not an error. If there are legitimately multiple role matches in a window, the error message should tell the user to add `--index N` rather than failing silently.
- id: virtiofs-serves-stale-cached-files-after-host-update
  title: VirtioFS serves stale cached files after host update
  category: bug
  status: not_started
  dependencies: []
  description: |
    VirtioFS shared filesystem can serve cached (stale) file content
    to the VM even after the host has written an updated version. Current workaround:
    use base64 transfer (`testanyware scp` or inline base64) or restart the VM. A
    `testanyware sync` command or explicit cache-bust mechanism (e.g. `testanyware scp
    --no-virtio-cache`) would eliminate this class of silent transfer error. Affects
    any workflow that edits a source file on the host and then recompiles or re-runs it
    inside the VM. Surfaced on 2026-04-17 during Racket sample app validation.
- id: add-doctor-check-for-usr-local-bin-testanyware-dev-symlink-shadowing-brew-install
  title: Add doctor check for /usr/local/bin/testanyware dev-symlink shadowing brew install
  category: feature
  status: done
  dependencies: []
  description: |
    Add a `testanyware doctor` check (or equivalent safeguard) for a `/usr/local/bin/testanyware` dev symlink that shadows the Homebrew-installed binary.

    **Problem:** During the P6 smoke test a stale dev symlink at `/usr/local/bin/testanyware` (created 2026-04-19, pointing to a `swift build -c release` artifact) shadowed `/opt/homebrew/bin/testanyware`. This caused the brew-installed CLI to be ignored silently. The symlink had to be removed manually before the smoke test could proceed.

    **Proposed fix — choose one or both:**
    1. **Doctor check:** Add a `testanyware doctor` subcommand (or extend an existing one) that warns when `which testanyware` returns something other than a path under `$(brew --prefix)`. Print a remediation hint (`sudo rm /usr/local/bin/testanyware`).
    2. **Build-time gate:** Document in `CONTRIBUTING.md` that `swift build` output should never be symlinked into `/usr/local/bin`; contributors should use `$(brew --prefix)/bin` exclusively after release testing.

    Option 1 is the higher-value safety net because it catches the hazard at runtime for any contributor, not just after reading the docs.
  results: |
    Implemented `testanyware doctor` as a top-level subcommand. Catches the
    P6 hazard: a `/usr/local/bin/testanyware` dev symlink shadowing the
    Homebrew install, silently overriding the user's intended brew CLI.

    ## What was built

    - `cli/Sources/TestAnywareDriver/Diagnostics/InstallPathCheck.swift` —
      Pure classifier (`InstallPathCheck.classify`) plus subprocess-driven
      runtime entry point (`InstallPathCheck.run`). Splitting the decision
      from the I/O lets us test classification deterministically against
      synthetic paths without needing a real Homebrew install.
    - `cli/Sources/testanyware/DoctorCommand.swift` — Thin CLI wrapper that
      invokes `InstallPathCheck.run`, prints a fixed-format report, and
      exits non-zero when a problem is detected.
    - `cli/Tests/TestAnywareDriverTests/Diagnostics/InstallPathCheckTests.swift`
      — 11 swift-testing cases covering: Apple Silicon healthy path, Intel
      healthy path, dev symlink under `/usr/local/bin`, swift-build
      artifact, adversarial near-prefix `/opt/homebrew2`, prefix with
      trailing slash, no Homebrew, not on PATH (with and without
      Homebrew), `CheckResult.isOK` mapping for benign/actionable verdicts.
    - Wired into `TestAnywareCLI.subcommands`.
    - Documented in `README.md` (CLI section), `docs/reference/cli-commands.md`
      (top-level table + dedicated section), and `LLM_INSTRUCTIONS.md` (VM
      lifecycle section, since doctor is an install-time concern).

    ## Design choices worth flagging

    1. **Path comparison uses the `which` result verbatim, not its
       `realpath`.** Originally I `resolvingSymlinksInPath`'d the on-PATH
       path to be "more accurate," then realised the user's remediation
       needs to point at the *symlink* itself (e.g. `sudo rm
       /usr/local/bin/testanyware`), not its target. Resolving symlinks
       would have made the remediation say `sudo rm
       /Users/dev/.../cli/.build/release/testanyware`, which is wrong —
       that target is rebuilt every `swift build`; deleting it doesn't fix
       the shadow. Test cases assume raw `which` output.

    2. **Trailing-slash guard on the prefix check.** `path.hasPrefix(prefix
       + "/")` instead of `path.hasPrefix(prefix)` so that
       `/opt/homebrew2/bin/testanyware` is correctly classified as
       *shadowed* (not a healthy `/opt/homebrew` install). Covered by
       `adversarialPrefixDoesNotMatchByStringPrefix`.

    3. **No Homebrew → `noHomebrew` is `isOK = true`.** Doctor cannot
       verify what it cannot compare against. A user running from source
       on a no-brew machine should not get a noisy failure from a hazard
       detector that has no reference point.

    4. **Out of scope: OCR status clear.** `OCRStatusFile.swift:4` carries
       a comment saying the file is "cleared by `testanyware doctor` on
       recovery" — that was aspirational and predates this task. I left
       the comment alone; wiring the OCR handling is a separate concern
       that the next person adding a doctor check can pick up by extending
       the same file (the structure already supports multiple checks).

    ## Verification

    - `swift build` — clean compile.
    - `swift test --skip IntegrationTests` — 316 tests, 28 suites, all
      pass. The new "InstallPathCheck.classify" suite contributes 11.
    - Healthy install, manual run:
      `testanyware doctor` → exits 0, prints `✓ install path is under
      Homebrew prefix`.
    - Shadow simulation via `PATH="$tmpdir:..." testanyware doctor` with
      a symlink in `$tmpdir`: exits 1, prints the `✗ … shadows …`
      diagnostic with the correct `sudo rm <symlink>` remediation.
    - `testanyware --help` lists `doctor` in the subcommands table.
    - `testanyware doctor --help` shows the abstract.

    ## What this suggests next

    - The doctor command pattern is now in place; cheap to extend. Likely
      next checks: (a) clear a degraded `OCRStatusFile` on recovery (per
      the existing `OCRStatusFile.swift:4` comment), (b) verify the
      bundled per-platform agents under `$(brew --prefix
      testanyware)/share/testanyware/agents/<platform>/...` exist and are
      executable, (c) verify `tart` / `qemu` availability at the paths
      used by `QEMURunner` and `TartRunner`. None of these are urgent;
      they are all consolidations of existing brittle resolution paths.
    - The contributor-facing companion (option 2 in the task description —
      "document in CONTRIBUTING.md that swift-build output should never
      be symlinked into /usr/local/bin") was not done. Doctor is now the
      runtime safety net; the docs nudge can be added if someone gets
      bitten again, but the runtime check is the higher-value half of the
      proposed pair, and that is what the task explicitly identified as
      the priority.
